package me.wopian.module.hatayama
/*
import androidx.room.*

@Dao
interface CountryDataDao {
    @Query("SELECT * from countryData WHERE countryData.isoCode = :countryCode")
    fun getCountryData(countryCode: String): List<CountryData>

    @Insert(onConflict = OnConflictStrategy.REPLACE)
    fun insert(countryData: CountryData)
}


 */